{
  "scripts": {
    "build": "esbuild assets/js/app.js --bundle --outdir=../priv/static/js --loader:.js=jsx --minify",
    "deploy": "npm run build && phx.digest"
  },
  "dependencies": {
    "@babel/preset-react": "^7.26.3",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "babel": {
    "presets": [
      "@babel/preset-env",
      "@babel/preset-react"
    ]
  },
  "devDependencies": {
    "esbuild": "^0.25.2"
  }
}
